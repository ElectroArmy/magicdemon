{
    "name": "ak",
    "version": "0.0.0",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "serve": "vite preview",
        "test": "echo \"Error: no test specified\" && exit 1",
        "deploy": "gh-pages -d dists"
    },
    "devDependencies": {
        "vite": "^2.5.4"
    },
    "dependencies": {
        "gh-pages": "^3.2.3",
        "three": "^0.132.2"
    }
}
